{
    "devCommand": "uvicorn main027:app --host 0.0.0.0 --port 3000",
    "builds": [
      {
        "src": "main027.py",
        "use": "@vercel/python"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "main027.py"
      }
    ]
  }